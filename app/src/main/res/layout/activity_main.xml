<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <WebView
        android:id="@+id/webView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:max="100" />

    <!-- Was a full-size FAB at bottom|end — directly on top of the web app's own "+" FAB and
         the bottom tab bar's Tasks label (a real layout collision, not a taste call). Moving it
         to a corner still under the web header collided with that instead (the web header spans
         the full top width — hamburger/title/overflow — so anything floating there overlaps
         something). This is a rare "re-enter device setup" action, not a primary one, so it
         docks inside the status bar strip itself instead: small, flat, no elevation, sized and
         positioned to stay above the web app's own header rather than floating over any of its
         content. Neutral color since it's not a category and shouldn't compete as a 4th accent. -->
    <ImageButton
        android:id="@+id/deviceSetupFab"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_gravity="top|end"
        android:layout_marginTop="4dp"
        android:layout_marginEnd="8dp"
        android:background="@android:color/transparent"
        android:padding="4dp"
        android:scaleType="fitCenter"
        android:contentDescription="@string/device_setup"
        android:src="@android:drawable/ic_menu_manage"
        app:tint="#9BA6A0" />

</FrameLayout>
